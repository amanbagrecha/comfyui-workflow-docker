# Pipeline-specific Python dependencies
# Baked into the Docker image at build time.
# To update: edit this file and rebuild the image.
#
# PyTorch is pinned here with the cu128 index so both the early torch bootstrap
# and the final pipeline install reuse the same CUDA wheels. Targets:
#   sm_89  — L40S, RTX 6000 Ada (Ada Lovelace)
#   sm_120 — RTX 5090 (Blackwell)
--extra-index-url https://download.pytorch.org/whl/cu128
torch==2.10.0+cu128
torchvision==0.25.0+cu128
torchaudio==2.10.0+cu128

# Numerics / image processing
numba
# Keep one OpenCV distribution in the image; contrib covers the base package.
opencv-contrib-python==4.12.0.88
matplotlib
polars

# Inpainting / CLI helpers
fire

# Utilities
click
requests
tqdm

# Privacy blur + object detection support deps
onnxruntime-gpu>=1.19.2
ultralytics-thop>=2.0.18

# 360 panorama conversion
pytorch360convert
